angular    .module('app')    .controller('UnitCtrl', function($scope) {        function hasClassCheck(element, cls) {              return (' ' + element.className + ' ').indexOf(' ' + cls + ' ') > -1;    }        $scope.$on('$viewContentLoaded', function()    {       // code that will be executed ...       // every time this view is loaded        var el = document.getElementById('checkUnits');        var isCelcius = hasClassCheck(el, 'Celcius');           $scope.UnitsUnits = isCelcius; // ini         console.log("Hello");         });                            });    